<p class="text-muted">A full audit trail of every SMS and email the portal has sent — bulk messages, dues reminders, absentee follow-ups, all of it.</p>

<x-docs.step :number="1" image="admin/communications/contact-log-1.png" alt="The contact log filtered list">
    Go to <strong>Contact Log</strong> in the sidebar. Messages are grouped by batch, so one bulk send shows as a
    single entry with its full recipient list rather than dozens of separate rows.
</x-docs.step>

<x-docs.step :number="2">
    Filter to find a specific send, then select <strong>Export</strong> for a CSV (with a content sample) or
    <strong>Print</strong> for a clean copy.
</x-docs.step>
